NetView

It is an alternate of `net view \\computername`, for easier getting the whole net share name (those with spaces) in batch scripts. Windows tiny util.

这是一个`net view \\computername`的替代，使批处理中易于获取共享的完整名称（含空格的）。Windows 小工具。
